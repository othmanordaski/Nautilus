# Core dependencies
rich>=13.0.0
httpx>=0.27.0
httpcore>=1.0.5
anyio>=4.4.0
beautifulsoup4>=4.11.0
questionary>=2.0.0

# Optional dependencies (install manually if needed):
# pip install lxml        — faster HTML parsing
# pip install yt-dlp      — video downloads (-d flag)

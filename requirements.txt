# Core dependencies
rich>=13.0.0
httpx>=0.24.0
beautifulsoup4>=4.11.0
questionary>=2.0.0

# Optional: faster HTML parsing (used by BeautifulSoup)
lxml>=4.9.0

# Optional: download support (used by --download / -d)
yt-dlp>=2023.0.0
